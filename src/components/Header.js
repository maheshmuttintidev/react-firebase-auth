import React from 'react';

export default function Header({children}) {
    return (
        <div className="w-100 header p-2 mb-5">
            <h1 className="text-center">Authentication using Firebased Auth API with React.js</h1>
        </div>
    );
}
